<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/iconartmaster"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconartmaster"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArtMaster"
        tools:targetApi="31">

        <activity
            android:name=".aisection.AiAssistantActivityPreview" />

        <activity
            android:name=".aisection.AiAssistantActivityIdeas"
            android:parentActivityName=".aisection.AiAssistantActivityPreview"/>

        <activity
            android:name=".aisection.AiAssistantActivityImg"
            android:parentActivityName=".aisection.AiAssistantActivityPreview"/>

        <activity
            android:name=".aisection.AiAssistantActivityText"
            android:parentActivityName=".aisection.AiAssistantActivityPreview" />

        <activity android:name=".favorites.FavActivity"/>

        <activity android:name=".tutorials.TutorialsPreviewActivity" />

        <activity
            android:name=".tutorials.TutorialsContentActivity"
            android:parentActivityName=".tutorials.TutorialsPreviewActivity" />

        <activity android:name=".paths.PathsActivity"/>

        <activity
            android:name=".home.HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.ArtMaster">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".login.Login"/>

        <activity android:name=".profile.ProfileActivity"/>

        <activity android:name=".notes.NoteActivity"/>

        <activity
            android:name=".notes.DetailActivity"
            android:parentActivityName=".notes.NoteActivity">
        </activity>

        <activity android:name=".alarm.TimePickerActivity"/>

        <activity android:name=".adminPanel.AdminPanelActivity"/>

        <activity android:name=".adminPaths.PathsActivity"/>

        <activity
            android:name=".adminPaths.DetailPathActivity"
            android:parentActivityName=".adminPaths.PathsActivity" />

        <activity android:name=".adminTutorials.TutorialsActivity"/>

        <activity
            android:name=".adminTutorials.DetailTutorialActivity"
            android:parentActivityName=".adminTutorials.TutorialsActivity" />

        <activity android:name=".adminUsers.UsersActivity"/>

        <activity
            android:name=".adminUsers.DetailUserActivity"
            android:parentActivityName=".adminUsers.UsersActivity" />

        <receiver android:name=".alarm.AlarmReceiver"/>
            
        <activity
            android:name=".login.Logout"
            android:exported="true"
            android:theme="@style/Theme.ArtMaster" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ArtMaster" />

        <activity android:name=".register.RegisterActivity" />

        <receiver android:name=".tutorials.AlarmReceiver" />

    </application>

</manifest>